<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Crear Cuenta</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/login.css" />
</head>

<body class="login-body">

  <div class="login-container shadow-lg">

      <h2 class="text-center mb-4 fw-bold text-primary">Crear Cuenta</h2>

      <% if (message) { %>
        <div class="alert alert-danger text-center"><%= message %></div>
      <% } %>

      <form action="/register" method="POST">

        <div class="mb-3 position-relative">
          <label class="form-label fw-semibold">Nombre completo</label>
          <input type="text" name="nombre" class="form-control form-control-lg" required placeholder="Tu nombre completo">
        </div>

        <div class="mb-3 position-relative">
          <label class="form-label fw-semibold">Nombre de Usuario</label>
          <input type="text" name="username" class="form-control form-control-lg" required placeholder="Crea un usuario">
        </div>

        <div class="mb-3 position-relative">
          <label class="form-label fw-semibold">Contraseña</label>
          <input type="password" name="password" class="form-control form-control-lg" required placeholder="Crea una contraseña">
        </div>

        <button type="submit" class="btn btn-primary w-100 btn-lg mt-3">
          Crear Cuenta
        </button>
      </form>

      <p class="text-center mt-4 mb-0">
        ¿Ya tienes una cuenta?  
        <a href="/login" class="fw-bold text-primary text-decoration-none">Ingresar</a>
      </p>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
