<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Iniciar Sesión</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Icons -->
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" />

  <!-- Custom CSS -->
  <link rel="stylesheet" href="/css/login.css" />
</head>

<body class="login-body">

  <div class="login-container shadow-lg">

      <h2 class="text-center mb-4 fw-bold text-primary">Ingresar al Sistema</h2>

      <% if (message) { %>
        <div class="alert alert-danger text-center"><%= message %></div>
      <% } %>

      <form action="/login" method="POST">

        <div class="mb-3 position-relative">
          <label class="form-label fw-semibold">Usuario</label>
          <input 
            type="text" 
            name="username" 
            class="form-control form-control-lg ps-5"
            required
            placeholder="Nombre de usuario"
          />
          <i class="fa-solid fa-user login-icon"></i>
        </div>

        <div class="mb-3 position-relative">
          <label class="form-label fw-semibold">Contraseña</label>
          <input 
            type="password" 
            name="password" 
            class="form-control form-control-lg ps-5"
            required
            placeholder="Ingresa tu contraseña"
          />
          <i class="fa-solid fa-lock login-icon"></i>
        </div>

        <button type="submit" class="btn btn-primary w-100 btn-lg mt-3">
          Iniciar Sesión
        </button>

      </form>

      <p class="text-center mt-4 mb-0">
        ¿No tienes una cuenta?
        <a href="/register" class="text-decoration-none fw-bold text-primary">Crear cuenta</a>
      </p>

  </div>

  <!-- Bootstrap JS -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

</body>
</html>
